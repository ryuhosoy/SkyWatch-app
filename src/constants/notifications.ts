/** 再接近として通知する半径（km） */
export const NOTIFY_RADIUS_KM = 40;

/** 一覧から消えた後、この時間経過で「離脱」とみなす（ms） */
export const ABSENCE_RESET_MS = 45_000;

/** 通知後、地図上で該当機をハイライトする時間（ms） */
export const NOTIFY_HIGHLIGHT_MS = 5_000;
